#  │                                       功能                                        │                            涉及文件                            │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 1   │ VL 带问题识图：VL 转发用户问题文字，不再用固定提示词                              │ protocol_proxy.rs                                              │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 2   │ VL max_tokens 可配：VisionRelayConfig.max_tokens 字段，默认 256                   │ settings.rs, protocol_proxy.rs, App.tsx                        │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 3   │ VL context_window：视觉模型上下文窗口限制，窗口外图片 strip 不调 VL               │ settings.rs, protocol_proxy.rs, App.tsx                        │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 4   │ VL 保存按钮：VL 设置区加显式保存按钮                                              │ App.tsx                                                        │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 5   │ VL image_url 格式修复：Responses 分支 image_url 用字符串而非 object               │ protocol_proxy.rs                                              │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 6   │ VL max_output_tokens：Responses API 用 max_output_tokens（非 max_tokens）         │ protocol_proxy.rs                                              │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 7   │ per-model reasoning strip：modelReasoningSupport map，Chat 格式 reasoning 处理    │ protocol_proxy.rs, settings.rs, App.tsx, model-windows.ts      │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 8   │ per-model 图片能力 map：modelImageSupport，大小写不敏感，map 非空时未列出默认     │ protocol_proxy.rs, settings.rs, App.tsx, model-windows.ts      │
  │     │ true                                                                              │                                                                │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 9   │ stripImages/map 冲突修复：map 非空时未列出模型不受 strip_images 影响              │ protocol_proxy.rs                                              │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 10  │ 大小写不敏感匹配：lookup_model_bool_support case-insensitive                      │ protocol_proxy.rs                                              │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 11  │ 前端 per-model 表格：textOnly + noReasoning 两列，列宽优化                        │ App.tsx, model-windows.ts, styles.css                          │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 12  │ 前端移除 stripImages checkbox：profile 级全局开关去掉                             │ App.tsx                                                        │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 13  │ 前端提示文字修改：3 处注释/提示更新                                               │ App.tsx                                                        │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 14  │ contextWindow 空值 UX：0 显示为空，占位符「留空不限制」                           │ App.tsx                                                        │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 15  │ 所有新增测试：82 个 protocol_proxy 测试（+30 个）+ model-windows 测试             │ tests/protocol_proxy.rs, model-windows.test.ts                 │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 16  │ http_client no_proxy：关闭系统代理避免拦截 127.0.0.1                              │ http_client.rs                                                 │
  ├─────┼───────────────────────────────────────────────────────────────────────────────────┼────────────────────────────────────────────────────────────────┤
  │ 17  │ settings.rs 初始化适配：ccs_import.rs, provider_import.rs, tests/launcher.rs      │ settings.rs, ccs_import.rs, provider_import.rs,                │
  │     │ 加字段                                                                            │ tests/launcher.rs                                   